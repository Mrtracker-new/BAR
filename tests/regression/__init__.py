"""
BAR Regression Guard Tests

These tests exist for ONE purpose only: catch regressions.
They verify that the current behaviour of critical code paths
stays stable across every future change.

Each test class maps to exactly one vulnerability fix in the
security implementation plan. Run the full suite before merging
ANY branch into main.

Author: Rolan Lobo (RNR)
"""
